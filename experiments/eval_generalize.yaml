id: label_extractor_generalization_eval
title: "Label Extractor generalization evaluation"
description: "Full config for evaluation experiment"
pythonpath: 
    - .././

# Uses xp_evaluation.py
file: xp_evaluation_generalization

# Experimental parameters
jobs_path: "/lustre/fswork/projects/rech/cdt/uqo57sk/experiments/entityrepresentations/jobs/labelextractor.learnlabelextractor"

hashs :
 - 3988c4dfbc9e1cddee560ec79ac0bcb39cf869fd6d712532376f90a75642acca

model_names: [
    # "gpt2-small",  # 12 (0-11) Transformer layers
    # "gpt2-medium", # 24 (0-23) Transformer layers
    # "gpt2-large",  # 36 (0-35) Transformer layers
    # "gpt2-xl",     # 48 (0-47) Transformer layers
    # "mistral-7B",  # 32 (0-31) Transformer layers
    "phi-2",       # 32 (0-31) Transformer layers
    ]

launchers: [
    # "duration=3h & cuda(mem=16G)*1 & cpu(cores=8)",    # v100 JZ
    # "duration=3h & cuda(mem=16G)*1 & cpu(cores=8)",    # v100 JZ
    # "duration=3h & cuda(mem=24G)*1 & cpu(cores=8)",    # v100 JZ
    # "duration=3h & cuda(mem=45G)*1 & cpu(cores=8)",    # A100 JZ
    # "duration=3h & cuda(mem=45G)*1 & cpu(cores=8)",    # A100 JZ
    "duration=3h & cuda(mem=24G)*1 & cpu(cores=8)",      # v100 JZ
    ]

batch_size: 10

dataset_name: "CoNLL2003"

with_context: True
layer: 20

layers_to_evaluate: 
    from: 0
    to: 31